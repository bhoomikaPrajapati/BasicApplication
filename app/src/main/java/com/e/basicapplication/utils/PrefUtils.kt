package com.e.basicapplication.utils

import android.content.Context
import android.content.SharedPreferences
import com.e.basicapplication.base.App.Companion.appContext
import com.e.basicapplication.data.connection.*


object PrefUtils {

/*

    private val pref: SharedPreferences? = appContext?.getSharedPreferences(sharedPreferences, Context.MODE_PRIVATE)
    private val editor: SharedPreferences.Editor = pref?.edit()

    fun setAuthorization(authorization: String?) {
        editor.putString(
            Authorization, authorization)
        editor.apply()
        editor.commit()
    }

    fun getAuthorization(): String {
        return pref.getString(
            Authorization, null) ?: ""
    }




    fun setShipOrderId(shipOrderId: String?) {
        editor.putString("shipOrderId", shipOrderId)
        editor.apply()
        editor.commit()
    }

    fun getShipOrderId(): String? {
        return pref.getString("shipOrderId", null)
    }


    fun setScanningForgings(scanningForgings: Boolean) {
        editor.putBoolean(
            ScanningForgings, scanningForgings)
        editor.apply()
        editor.commit()
    }

    fun isScanningForgings(): Boolean {
        return pref.getBoolean(
            ScanningForgings, false)
    }

    fun setDebugMode(debugMode: Boolean) {
        editor.putBoolean(
            DebugMode, debugMode)
        editor.apply()
        editor.commit()
    }

    fun isDebugMode(): Boolean {
        return pref.getBoolean(
            DebugMode, false)
    }

    fun logout() {
        editor.clear()
        editor.apply()
    }
*/
}
